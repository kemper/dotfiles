if [ -f `which brew && brew --prefix`/etc/bash_completion.d/git-completion.bash ]; then
  . `brew --prefix`/etc/bash_completion.d/git-completion.bash
fi
if [ -f `which brew && brew --prefix`/etc/bash_completion.d/tmux ]; then
  . `brew --prefix`/etc/bash_completion.d/tmux
fi
